[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "gersemi-extension-example"
description = "Gersemi extension example"
version = "0.0.1"
requires-python = ">=3.8"