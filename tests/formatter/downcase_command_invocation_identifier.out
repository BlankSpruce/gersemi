set(FOO BAR)

set(FOO BAR)

set(FOO BAR)

set(FOO BAR)

if(TRUE)
    foreach(FOO item1 ITEM2 item3)
        message(STATUS BAR)
    endforeach()
endif()

if(TRUE)
    foreach(FOO item1 ITEM2 item3)
        message(STATUS BAR)
    endforeach()
endif()

if(TRUE)
    foreach(FOO item1 ITEM2 item3)
        message(STATUS BAR)
    endforeach()
endif()

if(TRUE)
    foreach(FOO item1 ITEM2 item3)
        nessage(STATUS BAR)
    endforeach()
endif()
