load_cache(FOO READ_WITH_PREFIX BAR BAZ)

load_cache(FOO READ_WITH_PREFIX BAR BAZ FOO BAR BAZ)

load_cache(long_arg____________________________________________________________ READ_WITH_PREFIX long_arg____________________________________________________________ long_arg____________________________________________________________)

load_cache(long_arg____________________________________________________________ READ_WITH_PREFIX long_arg____________________________________________________________ long_arg____________________________________________________________ long_arg____________________________________________________________ long_arg____________________________________________________________ long_arg____________________________________________________________)

load_cache(FOO)

load_cache(FOO EXCLUDE BAZ INCLUDE_INTERNALS BAR)

load_cache(FOO EXCLUDE FOO BAR BAZ INCLUDE_INTERNALS FOO BAR BAZ)

load_cache(long_arg____________________________________________________________)

load_cache(long_arg____________________________________________________________ EXCLUDE long_arg____________________________________________________________ INCLUDE_INTERNALS long_arg____________________________________________________________)

load_cache(long_arg____________________________________________________________ EXCLUDE long_arg____________________________________________________________ long_arg____________________________________________________________ long_arg____________________________________________________________ INCLUDE_INTERNALS long_arg____________________________________________________________ long_arg____________________________________________________________ long_arg____________________________________________________________)